# This workflow has been superseded by nextjs.yml, which GitHub's Pages
# setup wizard generated. nextjs.yml is preferred because it uses
# actions/configure-pages@v5 with `static_site_generator: next`, which
# auto-injects the /SEMS basePath required for deployment to
# https://solarmason.github.io/SEMS.
#
# This file is kept as an empty workflow so it won't run.

name: (disabled) Deploy static site to GitHub Pages

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. See nextjs.yml."
